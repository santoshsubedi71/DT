<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Change</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>

<body class="d-flex justify-content-center align-items-center" style="height: 100vh; margin: 0;">

    <div class="container" style="position: absolute; top: 0;">
        <div class="row">
            <div class="col">
                <div class="card">
                    <div class="card-body">
                        <div class="text-center">
                            <h3>Change Your password here</h3>
                            {% if error is defined %}
                                <p class="text-danger">{{ error }}</p>

                            {% endif %}
                          
                        </div>
                        <form class="d-flex flex-column align-items-center" action="changePassword.php" method="POST">
                            <div class="mb-3 row">
                                <label for="oldPassword" class="col-sm-12 col-form-label text-center">Old Password</label>
                                <div class="col-sm-12 position-relative text-center">
                                    <input type="password" name="oldPassword" id="oldPassword" class="form-control" > <span class="text-danger">{{ error2 }}</span>                               
                                </div>
                            </div>
                            <div class="mb-3 row">
                                <label for="newPassword" class="col-sm-12 col-form-label text-center">New Password</label>
                                <div class="col-sm-12 position-relative text-last">
                                    <input type="password" name="newPassword" id="newPassword" class="form-control" >
                                </div>
                                 <span class="text-danger text-center">{{ error3 }}</span>
                            </div>
                            <div class="mb-3 row">
                                <label for="confirmPassword" class="col-sm-12 col-form-label text-center">Confirm New Password</label>
                                <div class="col-sm-12 position-relative">
                                    <input type="password" name="confirmPassword" id="confirmPassword" class="form-control" >
                                </div>
                        
                            </div>
                            <input type="submit" name="submit" value="Change Password" class="btn btn-primary">
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>

</html>
